;; on-the-fly compilation and checking
(require 'flymake)

;; show flymake error messages in minibuffer when the cursor is on a line with
;; a flymake error.  Saves tedious mouse usage
(load "flymake-cursor")
